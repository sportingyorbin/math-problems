function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}

// Example usage:
let randomNumber = getRandomInt(10);
console.log(randomNumber); // Output a random integer between 0 and max
